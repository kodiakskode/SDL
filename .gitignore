# Outreach backend — local runtime state, never committed
server/node_modules/
server/.env
server/*.db
server/*.db-shm
server/*.db-wal

# scripts/sync_xero.js's one dependency (libsodium-wrappers, for rotating
# its own GitHub secret) — installed fresh by the workflow each run
node_modules/
